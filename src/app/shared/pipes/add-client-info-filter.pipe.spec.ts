import { AddClientInfoFilterPipe } from './add-client-info-filter.pipe';

describe('AddClientInfoFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AddClientInfoFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
