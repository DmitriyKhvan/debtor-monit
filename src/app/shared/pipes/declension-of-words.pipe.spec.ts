import { DeclensionOfWordsPipe } from './declension-of-words.pipe';

describe('DeclensionOfWordsPipe', () => {
  it('create an instance', () => {
    const pipe = new DeclensionOfWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
